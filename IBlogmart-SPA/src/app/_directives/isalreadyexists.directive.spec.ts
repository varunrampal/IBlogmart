import { IsalreadyexistsDirective } from './isalreadyexists.directive';

describe('IsalreadyexistsDirective', () => {
  it('should create an instance', () => {
    const directive = new IsalreadyexistsDirective();
    expect(directive).toBeTruthy();
  });
});
